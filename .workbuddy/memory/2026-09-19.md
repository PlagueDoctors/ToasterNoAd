# 2026-09-19

## 只读项目发现（未修改任何文件）

用户要求：熟悉 NoAd 项目结构与代码，明确禁止修改。

- 项目：NoAd，Android 单模块应用，包名 `com.toaster.noad`，纯 Jetpack Compose + Material3。
- 技术栈：AGP 9.3.2 / Kotlin 2.2.10 / Compose BOM 2026.02.01 / Gradle Version Catalog；
  compileSdk 37、minSdk 30、targetSdk 37、JavaVersion.VERSION_11；release `optimization.enable = false`。
- 架构：feature-first + MVVM。`core/navigation`（底部导航4 Tab）、`core/designsystem`
  （StatCard、EmptyState）、`feature/{home,apps,logs,settings}`（Screen + ViewModel 成对）。
- 关键结论：**当前代码为纯 UI 骨架/原型**。全部数据来自 ViewModel `init` 中的硬编码假数据；
  无 Service、无无障碍服务、无 ActivityManager/通知监听、无 Room/DataStore/Preferences、
  无 Repository 层、无权限声明、无 Application 子类、无依赖注入。
- 能力缺口（与「自动关闭广告弹窗/启动广告」目标相关）：缺失的全部核心能力包括
  无障碍服务(AccessibilityService)、前台服务、`PACKAGE_USAGE_STATS`/`QUERY_ALL_PACKAGES` 权限、
  应用列表真实读取、广告拦截规则引擎、拦截记录持久化。
- 骨架硬编码/不一致问题（仅记录，未修改）：`HomeViewModel` 直接 set 假数据且 `ProtectedAppCount=6`
  与列表(6条)不一致、`AssetsViewModel` 的 "抖音/今日头条" 开关状态与 Home 列表不一致；
  `SettingsViewModel` `init` 覆盖了默认值；`HomeScreen` 的 "查看日志" onClick 为空；
  `strings.xml` 已定义完整中文字符串但代码中全部硬编码字面量未使用资源；
  `Theme.kt` 中 `SlateOutline` 被深浅色共用；`StatCard` 写死 `11.sp` 绕过 Typography；
  设置页「深色主题」开关无实际接线（Theme 用 `isSystemInDarkTheme()`）。
- 测试现状：仅 `ExampleUnitTest`（2+2）与 `ExampleInstrumentedTest`（包名断言）两个模板文件。
- 质量工具现状：**零配置**。无 Detekt/ktlint/Android Lint 配置、无覆盖率工具(Kover/JaCoCo)、无 CI 配置。
- 项目自带约定：`.trae/rules/rules.md` 详细规定了包层级、命名、Compose 参数顺序、测试命名等。
- 工作树：非 Git 仓库（无 `.git`），无版本控制基线。

## 新增开发规范文件（本日交付）

用户要求编写 Kotlin 编码规范 + Android 应用结构规范（Google 推荐），作为后续对话遵守的依据。
经确认落地为「双份」，严格程度为「官方强制 + 项目约束」。

- 新建 `docs/CODING_STANDARDS.md`（316 行）——完整规范。
  结构：A 部分 Kotlin 编码规范（源文件组织/命名/语言特性/协程/错误处理），
  B 部分 Android 应用结构规范（模块与目录/架构分层/组件与清单/依赖与构建/测试/质量工具/版本控制）
  + 附录 A 审查清单 + 附录 B 官方参考链接。
  每条规则标注 MUST/SHOULD/MAY，并内嵌本项目锁定事实表（minSdk 30、Compose、Version Catalog 等）。
- 新建 `.trae/rules/coding-standards.md`（166 行）——精简约束版提示词，供工具自动加载。
- 修改 `.trae/rules/rules.md`——在头部与尾部加入「规范优先级说明」与权威文件索引，
  确立 `docs/CODING_STANDARDS.md` > `.trae/rules/coding-standards.md` > `rules.md` 的权威顺序，
  消除同名规范冲突（原有 rules.md 未删除，保留作背景参考）。

规范中已显式标注的「现有待修正项」（供后续改造参考，本次未改代码）：
空 lambda 实现、StatCard 写死 fontSize、SlateOutline 深浅共用、界面硬编码中文而非用 strings.xml。

## 重要机制修正：规范必须放在 .workbuddy/skills/ 才对 WorkBuddy 生效

用户提问质疑「写在 .trae 目录的文件是否会一直被本对话参考」。经查证：

- `.trae/rules/` 属于 **Trae 编辑器** 的规则目录，**WorkBuddy 不会自动加载**。
  我最初把精简规范放在那里是判断失误（理由是「项目已有 .trae/rules/rules.md」，
  该理由只对 Trae 成立）。
- WorkBuddy 的自动加载机制是 **Skill**：
  - 项目级 `.workbuddy/skills/<name>/SKILL.md`（随仓库共享）
  - 用户级 `~/.workbuddy/skills/<name>/SKILL.md`
  - 加载依赖 frontmatter 的 `description` 与对话触发词匹配，属「按需加载」而非无条件常驻。
- `.workbuddy/memory/` 是记忆，用于跨会话保留工作事实，与规范约束职责不同。

已修正：创建项目级技能 `.workbuddy/skills/noad-dev-standards/SKILL.md`。
- frontmatter: `name: noad-dev-standards`，`agent_created: true`，
  description 覆盖全部触发场景（写 Kotlin/Compose 代码、改 Gradle、加权限、写测试、
  代码评审、命名与分层、资源与主题、协程写法等）。
- 内容为 `docs/CODING_STANDARDS.md` 的可执行摘要，并指向完整版供逐条核对。
- 通过 `quick_validate.py` 校验：Skill is valid。
- 已删除 init 脚本生成的 scripts/ references/ assets/ 模板目录。

结论：以后在本项目写代码时会自动加载该技能。
`.trae/rules/*.md` 保留给 Trae 使用，两者不冲突（Trae 读 .trae，WorkBuddy 读 .workbuddy）。

## 新增参考资料目录 reference/（本日交付）

用户要求新建参考目录，搜索现有安卓广告拦截方案汇总成 md，并给出初步实现方案。

- 新建 `reference/` 目录（用户指定「参考（英文）」，取英文名 reference）。
- 新建 `reference/android-ad-blocking-research.md`——方案调研汇总。
  覆盖：李跳跳（MissLee）、GKD、SKIP、TouchHelper、DNS/VPN/Hosts 方案；
  AccessibilityService 能力边界与 Android 12–15 限制演进；规则引擎设计对比表；
  法律合规要点（《互联网广告管理办法》第十条、不正当竞争风险、Play 政策限制）。
- 新建 `reference/IMPLEMENTATION_PLAN.md`——初步实现方案。
  含分层架构设计（新增 core/engine/、core/service/、core/database/、applist/）、
  规则模型与降级点击执行器伪代码、六阶段实施路线、风险表、
  5 个待用户决策问题、以及「先做 150 行 MVP 验证」的建议。

### 关键调研结论（供后续实现参考）

- 免 Root 前提下，拦截**弹窗/启动广告**只有 AccessibilityService 一条路。
  DNS/VPN/Hosts 解决的是网页/网络层广告，属不同问题域。
- 核心技术要点：
  1. `performAction(ACTION_CLICK)` 常失败（广告按钮多被容器包裹、clickable=false），
     需**向上找可点击祖先**，再降级到 `dispatchGesture` 坐标点击。
  2. 规则应**双限定 packageName + activityName**（借鉴 SKIP），开屏广告只在特定 Activity，
     可大幅降低误点。
  3. `onAccessibilityEvent` 在**主线程**执行，必须最快失败 + 异步写库 + 设置 notificationTimeout。
  4. Android 13+ 侧载应用的无障碍开关被 `Restricted settings` 拦截，
     需引导用户手动「允许受限设置」——NoAd minSdk 30 必然遇到。
- 法律风险高于技术风险：李跳跳因不正当竞争律师函停止更新、被 360 商店下架。
  GKD 用 GPL-3.0 + 免责声明划边界。建议定位个人/开源工具，坚持纯本地不联网。
- GKD 可借鉴的架构：选择器引擎独立模块、隐藏 API 集中封装、规则与代码分离（默认不带规则）。

## 三策略综合方案（本日交付）

用户要求：在 app 内**同时实现**无障碍模拟点击 + DNS/Private DNS 过滤 + 本地 VPN 流量过滤；
本地 VPN 在非 VPN 使用时生效，**有其他 VPN 运行时让位**；定位个人/开源，暂不担法律风险。

- 新建 `reference/THREE_STRATEGY_PLAN.md`——三策略综合技术方案。

### 核心架构结论（重要）

**关键约束**：Android 官方明确规定「同一时刻只能有一个 VPN 连接运行，
新连接建立时旧连接被停用」，被抢占方收到 `onRevoke()`。
→ 因此 **S2(DNS) 与 S3(全流量) 必须设计为互斥模式，不能同时启用**。
  用 `NetworkFilterMode { OFF, DNS_ONLY, FULL_TRAFFIC }` 单开关控制。
→ 但 **S1(无障碍) 与 S2/S3 可同时开启**，因为无障碍服务不占用 VPN 资源。

**让位机制**（用户核心诉求）：
- 用 `NetworkCapabilities.hasTransport(TRANSPORT_VPN)` 检测其他 VPN（排除自身 handle）
- `onRevoke()` 中**绝不自动重连**——否则与其他 VPN 形成无限抢占循环
  （这是 Android 生态「VPN 反复掉线」的典型成因）
- 让位后在 UI 明示，恢复需用户显式触发，不做后台自动恢复

**S2 DNS-only 的精髓**：`VpnService.Builder` 只 `addRoute("10.0.0.1", 32)`
而不 `addRoute("0.0.0.0", 0)`，则仅 DNS 查询进 TUN，网页/下载/视频走原路径，性能影响极小。

**两个必须做对的细节**：
1. 上游 DNS 必须在 `establish()` **之前**读取，否则读到的是自己的虚拟 DNS（死循环）
2. 转发 socket 必须 `protect()`，否则 DNS 请求回流进自己 TUN

**S3 复杂度控制建议**：完整 TCP/UDP 转发是重型工程，建议先用「方案 C：域名级丢包」
（解析 DNS 得域名 → 命中黑名单则丢弃对应 IP 后续连接），避免自实现 TCP 栈。

### 交付内容结构

方案含：单一 VPN 约束与架构决策、VPN 检测与让位仲裁代码、三策略协同图、
能力矩阵（诚实标注信息流原生广告三策略均无法拦截）、
S2 路由/包处理/上游 DNS/NXDOMAIN 构造、S3 路由与三种实现路径对比、
统一控制状态模型与 UI 草案、域名规则引擎（白名单优先 + 后缀匹配防误判）、
性能功耗设计、六阶段（A–F）实施路线、10 项风险清单、5 个待决策问题、
关键技术决策速查表。

### 待用户决策

Q1 默认模式（DNS_ONLY / FULL_TRAFFIC）、Q2 S3 实现深度、Q3 内置域名规则规模、
Q4 是否保留「仅 S1」快速模式、Q5 是否引入前台服务保活。

---

## 新增：Shizuku 可选增强方案（本日交付）

用户追加提问：给 Shizuku 额外权限的手机能否做优化。交付
`reference/SHIZUKU_ENHANCEMENT.md`。

### 调研确认的 Shizuku 事实（已核实）

- 原理：用户一次 ADB 启动 Shizuku Server（**UID 2000 shell**，非 root）；
  App 通过 `ShizukuBinderWrapper` 把系统 API 调用转发给 Server 执行。
- **边界**：重启后失效；不能授予 `dangerous` 权限（`pm grant` 无效）；
  不能读其他 App 沙盒；普通 App 进程内仍受 Hidden API 限制；
  shell 权限随 Android 版本 / 厂商 ROM 漂移。
- 官方已明确 **`newProcess` 将被移除，应改用 `UserService`**
  （newProcess 走文本通信，低效且不可靠）。
- `UserService`：在独立进程运行自有代码，UID 2000（ADB）/ 0（root），
  不受非 SDK 接口限制，适合长驻状态与重复 Binder 调用。

### ⭐ 最重要发现：Chain-3 绕过 VPN 互斥

`ShizuWall`（开源，无需 root）已验证：通过 Shizuku 调用 connectivity 服务的
**Chain-3 接口**，可按包名切断联网，**不建 TUN、不占 VPN、可与其他 VPN 共存**。

**这直接解决了主体方案最硬的约束**（S2/S3 因单一 VPN 限制而互斥）。
据此新增「**S4 应用级断网（APP_FIREWALL）**」模式，并让 `VpnArbitrator`
在检测到其他 VPN 时**从「完全让位」升级为「降级到 S4」**。

### 文档覆盖的 6 项优化

| 优化 | 价值 |
|---|---|
| Chain-3 单应用断网 | 🔴🔴 架构级，解决 VPN 互斥与让位后失效 |
| Private DNS 直接改写（`WRITE_SECURE_SETTINGS`） | 🔴🔴 零代码替代 S2，不占 VPN |
| 绕过 Android 13+ 侧载受限设置 | 🔴 侧载必遇问题，体验提升大 |
| 冻结/隐藏/组件停用（`setComponentEnabledSetting`） | 🟡 根治开屏广告（S1 只能等它出现再点） |
| AppOps 权限拒绝 | 🟡 削弱广告 SDK 保活/悬浮窗/设备标识 |
| `forceStopPackage` 强制停止 | 🟡 S1 兜底，但风险最高，建议默认关闭 |

### 设计原则（已写入文档）

1. **Shizuku 是可选增强，不是前提** —— 无 Shizuku 设备核心功能必须完整可用。
2. 每项能力**独立运行时探测 + 独立降级**，不做「有 Shizuku 就有一切」的静态假设。
3. 增强项**默认全部关闭**，由用户显式开启，UI 如实标注状态。
4. `core/shizuku` 只被 Service/Repository 层引用，禁止 `feature/` 直接调用。

### 新增待决策

SQ1 是否引入 Shizuku 依赖、SQ2 Chain-3 是否作为正式 S4 纳入主体方案、
SQ3 Private DNS 与 S2 如何共存、SQ4 是否做组件停用、
SQ5 是否提供强制停止、SQ6 增强项默认关闭 vs 一键启用推荐项。

---

# 阶段 A 基础设施实现（代码交付）

用户指令：「先按你的阶段A实现基础设施搭建代码，目前的ui界面不是固定的，根据实际需求任意修改」
→ 已获授权修改 UI，并明确指出界面可按实际需求重构。

## 构建配置变更

`gradle/libs.versions.toml` +`app/build.gradle.kts` +`build.gradle.kts` +`gradle.properties`：

- 新增 KSP **2.2.10-2.0.2**（与 Kotlin 2.2.10 严格配对）
- 新增 Room **2.8.5**（runtime + ktx + compiler）、DataStore **1.2.1**、coroutines **1.9.0**
- lifecycle-viewmodel-ktx（原有 lifecycle 变量 2.8.7）
- 开启 `buildFeatures.buildConfig = true`
- Room schema 导出到 `app/schemas/`，`room.generateKotlin = true`

### ⚠️ 踩坑（务必记住）

**AGP 9.x 构建直接失败**：`Using kotlin.sourceSets DSL to add Kotlin sources is not
allowed with built-in Kotlin.` —— KSP 以 `kotlin.sourceSets` 注册生成目录，
被 AGP 9.x 内置 Kotlin 支持拒绝。
**修复**：`gradle.properties` 加 `android.disallowKotlinSourceSets=false`。

## 新增代码结构

```
core/
├── model/          SkipRule+TargetType+MatchMode+AdType+InterceptSource+InterceptLog+TargetApp
│                   DomainRule+DomainMatchType+DomainCategory+DomainPolicy+DomainVerdict
│                   NetworkFilterMode+VpnYieldReason+AccessibilityState+ProtectionState
├── database/       entity/{SkipRule,DomainRule,InterceptLog,TargetApp}Entity
│                   dao/{SkipRule,DomainRule,InterceptLog,TargetApp}Dao
│                   EnumConverters · NoAdDatabase · Mappers.kt
├── data/
│   ├── repository/ RuleRepository · DomainRuleRepository · LogRepository · TargetAppRepository
│   └── settings/   SettingsRepository（DataStore）+ AppSettings
├── engine/         DomainRuleEngine（白名单优先 + 后缀防误杀）
├── applist/        InstalledAppDataSource（真实读取已安装应用）
├── designsystem/   NoAdSection · SectionHeader · IconBadge · KeyValueRow
└── navigation/     NoAdViewModelFactory（手写 ViewModel 工厂）

feature/network/    新增「网络过滤」页（模式单选 + 让位提示 + 能力边界说明）
NoAdApplication.kt  AppContainer（手写依赖容器）+ 启动初始化
```

## 关键实现决策

1. **手写依赖容器**（`AppContainer`，全 `lazy`）而非引入 Hilt/Koin ——
   单模块、依赖图浅，DI 框架的注解处理收益不划算。
   已记录：ViewModel 超过约 15 个时应重新评估。
2. **领域模型不依赖 Room 注解**，转换集中在 `Mappers.kt`。
3. **枚举以 String 存库**（非序号），避免枚举增删导致存量数据错位。
4. **主键/索引按热路径设计**：`skip_rule.package_name` 建索引（S1 每事件都要按包名取规则）；
   `domain_rule.pattern+is_whitelist` 唯一索引（防重复导入）；`intercept_log.timestamp` 索引。
5. **`DomainRuleEngine` 不可变 + 预编译哈希集合**：匹配为 O(域名层级数)，
   可被 S2 包处理线程与 S3 转发线程无锁并发读取；
   规则变更时由 Repository 重建实例整体替换（Mutex 串行化写路径）。
6. **后缀匹配正确实现**：逐级剥离子域（`a.b.example.com` → `b.example.com` → `example.com`），
   而非 `endsWith` —— 后者会让 `badexample.com` 命中 `example.com`。
7. **应用管理语义**：开关关闭 = **删除**纳管记录，而非置 enabled=false，
   使「受保护应用数」语义清晰。
8. **UI 重构**：首页新增「三策略状态」列表（如实标注未生效原因而非笼统显示"已保护"）；
   网络页把互斥约束表达为**单选分组**（从交互层杜绝冲突配置），
   并明确标注每种模式是否占用 VPN；新增底部导航「网络过滤」等入口。

## 验证证据（本次有效）

```
./gradlew :app:assembleDebug :app:testDebugUnitTest  →  BUILD SUCCESSFUL in 55s
DomainRuleEngineTest: tests=15 failures=0 errors=0
ExampleUnitTest:      tests=1  failures=0 errors=0
KSP 生成 5 个 Room _Impl 类（4 DAO + Database）
Room schema 已导出：app/schemas/com.toaster.noad.core.database.NoAdDatabase/1.json
APK：app/build/outputs/apk/debug/app-debug.apk（63M）
```

## 本次未做（阶段 A 范围外）

- 未实现任何 Service（S1 无障碍、S2/S3 VpnService 均未创建）
- 未实现 S1 的 UiMatcher / ClickExecutor
- 未实现 `VpnArbitrator`（让位仲裁）
- 未实现 Shizuku 特权通道
- 首页「三策略状态」的 `active` 目前硬编码 false，待服务接入后替换
- 「较昨日对比」指标暂未展示：DAO 缺区间计数查询，不用近似值误导用户

## 遗留待办

1. `LogRepository` 缺「昨日区间计数」查询 → 补齐后恢复首页「较昨日」对比
2. ~~`BuiltinDomainRules` 仅 4 条占位规则~~ → **本日已定稿**（见下）
3. `apps` 页应用图标仍是占位方块，待接入 `ApplicationInfo` 图标加载
4. 尚未引入 Detekt / ktlint（阶段 F）

---

# 三项收尾交付（用户指令：定好 BuiltinDomainRules + 同步方案文档 + 更新 Shizuku 内容 + 选图标）

## 交付一：BuiltinDomainRules 定稿

**决策**：规则从 Kotlin 常量改为 `assets/rules/builtin_domains.json`，加载器解析后导入 Room。

- 新建 `app/src/main/assets/rules/builtin_domains.json` —— **62 条黑名单 + 6 条白名单**
- 新建 `core/data/rules/BuiltinRulesLoader.kt` —— 解析器
  - 坏数据**单条跳过**而非整体丢弃，如实上报 `skipped` 计数
  - 未知分类退回 `AD`（而非丢规则），避免新增分类导致规则静默失效
- `DomainRuleRepository` 新增 `importBuiltin(blacklist, whitelist)` / `replaceBuiltin` / `clearBuiltin`
  / `builtinCount` / `builtinWhitelistCount`，返回 `BuiltinImportResult`
- `DomainRuleDao` 新增 `countWhitelistBySource`
- `NoAdApplication.initializeDomainRules()` 改为**同时检查黑白名单是否缺失**
  （原先只查黑名单，会让「白名单丢失但黑名单存在」的中间态永远不被修复）

**白名单的用途（关键设计）**：文件中的白名单不是"例外列表"，而是**显式声明的风险权衡**。
例：友盟 `umeng.com` 同时承载崩溃上报与分享回调，放进黑名单可能破坏某些 App 的分享功能，
因此以白名单形式登记该权衡并在 `note` 写明理由。
**若只导入黑名单不导入白名单，这层防护会静默失效。**

**选取标准**（写入 JSON `meta.criteria`，作为后续扩充硬约束）：
只收纯粹广告/追踪域；排除业务/登录/支付/内容域；排除争议全家桶主域；
每条必须有 note；规模精简，优先零误杀。

**测试（29 个新增断言）**：
- `BuiltinRulesLoaderTest`（16）—— 解析器容错
- `BuiltinRulesAssetTest`（13）—— **对真实文件做契约测试**
  - 图案格式（无协议/通配符/路径/大写/尾点）
  - 无重复、分类合法、每条有 note
  - **白名单条目确实不被拦截**（误杀防护生效）
  - **业务域名确实不被拦截**（google.com / weixin.qq.com / taobao / github 等）
  - 规模约束（<200 条）

> `BuiltinRulesAssetTest` 是这里最关键的设计：只测解析器的话，
> 文件本身写错（误把业务域名加进黑名单）不会有任何测试失败，错误会直接进用户设备。

## 交付二：应用图标（零版权风险）

**决策**：**完全原创矢量**，不引用任何第三方图标库图案。

调研确认（已联网核实）：Material Symbols 是 Apache-2.0、Lucide 是 ISC、Phosphor 是 MIT，
**三者都可商用但都需保留版权与许可声明**。对启动器图标而言，
把第三方声明塞进「关于」页是不必要的合规负担（图标常被拆出去用于 README/商店页），
因此选择自绘。

**图形**：白色方框被 45° 斜杠在正中心切断 = 「被拦截的广告位」
（刻意避免盾牌与禁止符号：前者语义不精确，后者过于通用、小尺寸无法区分）。

**几何**（方框 26..82 环宽 7 圆角 10；斜杠宽 7；缺口半宽 5，中心恰在 54,54）：
- 方框描边与斜杠**等宽（都是 7）** —— 早期用 9 宽斜杠会压过方框，
  读起来像"被划掉的禁止符号"而非"被拦截的广告位"
- 方框完全落在安全区（18..90）内；斜杠有意延伸到画布外，由各厂商遮罩裁切

**交付文件**：
- `res/drawable/ic_launcher_background.xml`（渐变 + 两处径向柔光）
- `res/drawable/ic_launcher_foreground.xml`
- `res/drawable/ic_launcher_monochrome.xml`（Android 13+ 主题化图标，独立文件）
- `res/mipmap-anydpi/ic_launcher{,_round}.xml`
- `res/mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher{,_round}.png`（10 个位图）
- `tools/gen_icon_paths.py` / `tools/gen_icon_pngs.py`
- `reference/preview_{square,round,48_zoomed}.png`（大图预览）

**三条硬约束（已写入文件注释）**：
1. **单色兼容**：图形只依赖 alpha，缺口靠「背景透出」而非「填背景色」。
   用背景色填缺口会让单色模式出现实色补丁，图形反而被破坏。
2. **不用 `strokeWidth`**：中空方框用「外轮廓 + 内轮廓 + evenOdd」表达。
   部分启动器生成单色主题图标时会丢弃 stroke 只保留 fill。
3. **矢量与位图必须同步**：几何参数同时存在于 foreground.xml 与 gen_icon_pngs.py。
   另 `ic_launcher_monochrome.xml` 必须与 foreground **完全一致**。

**已删除**：`mipmap-*dpi/ic_launcher{,_round}.webp`（Android Studio 模板机器人图标）——
否则会出现「应用内是新图标、桌面还是机器人」的割裂。

**`colors.xml` 重写**：移除模板残留的 `purple_200/500/700`、`teal_200/700`（无任何引用），
改为登记图标配色常量并注明「应用内语义色一律在 `ui/theme/Color.kt`，此处只放图标色」。

### 踩坑记录（图像生成）

沙箱**无网络**，`pip install Pillow` 失败（`No matching distribution found`）。
→ 改用**纯 Python 标准库**（`zlib` + `struct`）手写 PNG 编码器，
图形用**解析式 SDF + 4x4 超采样**光栅化。

期间出现两个 bug：
1. `shade()` 注释说返回 0..1 但实际返回 0..255，`render()` 又乘了一次 255 → 全白。
   修正：`shade()` 明确返回 0..255，采样平均后不再乘。
2. 圆形遮罩最初写在逐像素循环里，与超采样互相干扰 → 拆成独立的 `apply_round_mask()`。
3. 内圆角半径最初与外圆角相同（都用 9），导致方环四角比四边细 →
   正确的内圆角 = 外圆角 - 环宽（10 - 7 = 3）。

## 交付三：THREE_STRATEGY_PLAN.md 重构（700 → 1610 行）

按用户要求把该文档确立为**唯一权威方案**，并合并 Shizuku 内容。

**主要变更**：
- 标题改为「四策略综合拦截技术方案」，新增 §0 结论摘要表
- **§2 约束表述修订**：明确「单一 VPN 约束**只作用于 VpnService**，
  S4 走 NetworkPolicyManager 不受此约束」——这是相对早期版本最重要的修订
- **§3.1 架构图**新增 `core/shizuku/` 包与阶段标注
- **§6 全新章节**（约 500 行）：S4 Shizuku 特权通道，合并原 SHIZUKU_ENHANCEMENT.md 全部内容
  - 6.1 Chain-3（含新增 §6.1.4 对策略架构的影响）
  - 6.2 能力边界 · 6.3 接入方式 · 6.4 Private DNS · 6.5 侧载限制解除
  - 6.6 组件停用 · 6.7 AppOps · 6.8 强制停止
  - **6.9 与 VpnArbitrator 整合**（让位后降级到 S4）
  - 6.10 更新后的策略矩阵
- **§8.4 新增「内置规则方案（已定）」**：记录 assets JSON 决策、规模、
  选取标准、白名单用途、两个测试类的职责分工
- **§8.3 后缀匹配**改为记录实际实现（逐级剥离子域 + HashSet 精确比较），
  说明比 `endsWith(".$s")` 更快且天然防误匹配
- **§10 阶段 A 标注已完成**，附「计划 vs 实际」差异表与完整验证证据
- **§10 新增阶段 F（S4 Shizuku 增强）**，并在原 F 改为 G
- **§11 风险清单拆为通用 + Shizuku 专属两张表**
- **§12 新增 Shizuku 实施优先级表**（P0 探测 + 侧载解除 / P1 Chain-3 + Private DNS / …）
- **§13 全新「设计原则」章节**（Shizuku 可选、独立降级、UI 如实标注、UserService 优先）
- **§14 待决策更新**：Q3 已关闭（标注关闭原因），SQ2 已采纳，其余保留并补建议
- **§15 全新「应用图标」章节**：结论、为何原创而非引用图标库（含三库许可对比）、
  设计说明、交付物清单、关键实现约束
- **§16 速查表扩充**：新增 S4 相关、内置规则、图标方案等多行

**`SHIZUKU_ENHANCEMENT.md` 归档**：头部加醒目提示「已合并，不再单独维护」，
并列出合并时做的 4 项修订，指向主体方案 §6。

## 验证证据（clean build）

```
./gradlew clean testDebugUnitTest assembleDebug  →  BUILD SUCCESSFUL in 18s

45 tests, 0 failures, 0 skipped
├── DomainRuleEngineTest        15
├── BuiltinRulesLoaderTest      16
├── BuiltinRulesAssetTest       13
└── ExampleUnitTest              1

APK 内确认（app-debug.apk，66MB）：
  assets/rules/builtin_domains.json          (8114 B)
  res/drawable/ic_launcher_foreground.xml    (1272 B)
  res/drawable/ic_launcher_monochrome.xml    (1272 B)
  res/mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher{,_round}.png
  → 无 *.webp 残留
```

## 本次之后的遗留待办

1. `LogRepository` 缺「昨日区间计数」查询（首页「较昨日」对比）
2. `apps` 页应用图标仍是占位方块，待接入 `ApplicationInfo` 图标加载
3. 规则页尚未实现（导入 JSON / 用户自定义增删改；Repository 层已就绪）
4. 尚未引入 Detekt / ktlint
5. 待决策 Q1（默认模式）、Q2（S3 深度）、Q4（仅 S1 快速模式）、Q5（前台服务保活）、
   SQ1/SQ3/SQ4/SQ5/SQ6（Shizuku 相关）

---

## 阶段 B：S1 无障碍实现（已完成）

用户要求：实现阶段 B，完成后可由用户自行实机调试。工作模式 Agent（自由修改 UI）。

### 新增文件（12 个主源码 + 2 个测试）

`core/engine/ui/`：
- `NodeSnapshot.kt` —— 节点只读快照。**核心决策**：`AccessibilityNodeInfo`
  无法在 JVM 单测中构造、必须成对 recycle、只在回调期有效。不抽象则
  "该不该点这个节点"完全无法测试，而 S1 是四种策略里唯一代替用户操作界面的。
- `NodeRecycler.kt` —— `recycle()` 自 **API 33 起弃用**（框架接管生命周期），
  但 `minSdk=30` 意味着 API 30–32 上不回收是**真实原生内存泄漏**。
  集中做版本分支，而非在调用点 `@Suppress` 后删掉回收调用。
- `UiTreeScanner.kt` —— BFS（非 DFS，因匹配需"浅层优先"），上限 2000 节点防 ANR，
  跨应用节点不展开子树，队列剩余节点必须 drain 回收。
- `UiMatcher.kt` —— 纯逻辑匹配。VIEW_ID > TEXT > DESCRIPTION > COORDINATE。
  viewId 支持短名匹配（`substringAfterLast('/')`），换包名后规则仍可用。
  COORDINATE 不"找最近节点"而是合成虚拟节点 —— 强行就近匹配会把点击目标改到别处。
- `AntiMisclickGate.kt` —— 三重闸门：规则冷却 3s + 节点冷却 5s + 全局节流 400ms。
  成本不对称：漏点=广告没关掉，误点=可能触发付费。有 prune 防记账泄漏。
- `ClickExecutor.kt` —— 三级降级：节点自身 → 向上找可点击祖先（≤5 层）→ 坐标手势。
  "向上找祖先"是关键：广告跳过 TextView 自身 `clickable=false`，响应的是父容器。

`core/repository/S1RuleCache.kt` —— 按包名分组的不可变快照（AtomicReference），
  由 Flow 重建。事件回调在主线程，禁止查库。

`core/service/`：
- `NoAdAccessibilityService.kt` —— 不做保活（无障碍是系统级服务，额外保活是画蛇添足）。
  动态设置 serviceInfo（部分 ROM 忽略 XML 的 flags）。
- `AccessibilityStateHolder.kt` —— StateFlow 发布真实状态。双轨：服务回调为权威 +
  解析 `ENABLED_ACCESSIBILITY_SERVICES` 为兜底。侧载受限判定**保守**：
  已授权时绝不标记为受限，否则损害 UI 可信度。
- `ProtectionFlags.kt` —— AtomicBoolean 内存镜像，热路径读它而非 DataStore。
- `AccessibilitySettingsLauncher.kt` —— 三级降级跳转，必须吞 `ActivityNotFoundException`。
- `event/EventProcessor.kt` —— 四道闸门：包名 → 事件类型 → Activity → 防误点。
  回调声明为 `suspend`，由 EventProcessor 在 scope 中启动协程 —— 避免实现方用 `runBlocking`。

### 修改文件

- `AndroidManifest.xml` —— 注册服务（`BIND_ACCESSIBILITY_SERVICE` + exported + meta-data）
- `res/xml/accessibility_service_config.xml` —— 新建，含 `canPerformGestures="true"`
- `ProtectionState.kt` —— `AccessibilityState` 从 3 字段扩为 4：新增 `appSwitchEnabled`，
  加 `isEffectivelyActive` / `needsSystemPermission`。三字段必须分开，
  否则用户看到"已开启"却无拦截记录，不知道哪一环断了。
- `HomeViewModel` —— 改继承 `AndroidViewModel`（需 Context 查系统设置）。
  **6 路 combine 触发 Kotlin 只到 5 路的限制**，改为先合并 5 路成 `AppData` 再二次合并。
- `HomeScreen.kt` —— `active` 不再写死 false；三态文案（运行中/待开启/未接入）；
  新增引导卡片；用 `LifecycleEventObserver` 在 ON_RESUME 重新核对授权
  （系统无相关广播，只能回前台时主动查）。
- `SettingsScreen/ViewModel` —— 新增 S1 开关与策略分组；副标题三段式说明卡在哪一环。
- `TargetAppDao` / `TargetAppRepository` —— 新增 `observeAccessibilityEnabled()`
- `SettingsRepository` —— 新增 `accessibilityEnabled: Flow<Boolean>`
- `NoAdApplication` —— 容器持有 `s1RuleCache` 与 `applicationScope`（供跨服务生命周期订阅）
- 首页转向设置页的接线在 `NoAdApp.kt` 用 `LocalContext`

### 验证

```
./gradlew clean testDebugUnitTest assembleDebug  → BUILD SUCCESSFUL in 12s
82 tests / 0 failures / 0 skipped（新增 37 个）
编译告警 0（修复了 recycle / LocalLifecycleOwner / ArrowForward 三处弃用）
APK 66MB，已校验含 accessibility_service_config.xml 与合并清单中的服务声明
```

### 踩坑

- 6 路 `combine` 的类型化重载只到 5 路，超出即退化为 `Array<Any>` 丢失全部类型
- Kotlin 中 `var` 捕获进 lambda 无法智能转换，需先赋给非空 `val`
- 校验 APK 内清单**不能**在二进制 `AndroidManifest.xml` 里搜字符串（AAPT2 编码进资源池），
  应看 `build/intermediates/merged_manifest/.../AndroidManifest.xml`
- 写代码时误用非 ASCII 标识符（`val 限定`），已改

### 交给用户实机调试前的三个要点

1. S1 生效需**两层授权**：系统设置授权 AND 应用内开关，缺一不可
2. Android 13+ 侧载限制：无障碍列表可能看不到 NoAd，需「允许受限设置」放行
   （本阶段只做文案引导，Shizuku 自动解除属阶段 F）
3. **默认不纳管任何应用** —— 排查"没效果"先确认已在应用管理页勾选目标应用

---

## 补充：README 初版 + 修正一处既存错误

用户要求：写一个简易 README，只做初步介绍，项目完成后补充。

### 产出

`README.md`（135 行），含：这是什么 / 目前能做什么（能力状态表） / 四种策略分工 /
如何使用（安装 + S1 两层授权 + 实机排查） / 构建环境 / 技术栈 / 项目结构 /
文档索引 / 已知限制 / 许可（待补充）。

**刻意保留的诚实标注**：S2/S3/S4 明确写「未开始」而非含糊带过；
信息流广告拦截不了写进「已知限制」；Android 13+ 侧载限制单独提示。

### 修正一处我此前引入的错误

**内置规则条数是 60 条黑名单，不是 62 条。** 我在上一轮的交付说明与文档中
写成了 62（凭空多算 2 条），本轮用脚本核验 JSON 时发现。
已修正 4 处：`THREE_STRATEGY_PLAN.md` 3 处、`MEMORY.md` 1 处。

同时核验了黑白名单重叠：`ironsrc.com`、`umeng.com` 同时出现在两个列表中，
这是**刻意设计**（白名单优先，避免误杀这两个同时承载崩溃上报/分享回调的域名），
不是数据错误。

### 又一处执行阶段的核对

写 README 时怀疑「应用管理页勾选」是否真的能让 S1 生效 ——
查 `AppsViewModel.toggleApp()` 确认它 upsert 的 `TargetApp` 中
`accessibilityEnabled = true`，与 `S1RuleCache` 读取的字段一致。
另有独立的 `setAccessibilityEnabled()` 可单独关掉某应用的 S1 而保留纳管关系。
README 中的操作步骤因此是准确的。

---

# 🔴 S1 实机完全无效果 —— 根因修复（本次最重要的交付）

## 用户报障

> "安装后启动成功，无障碍权限获取成功，在应用管理启用全部应用拦截，
> 但是实际打开其他软件时没有自动跳过广告，并且拦截日志全部为空，
> 拦截统计始终为0，测试多款软件打开都是如此 淘宝、京东、bilibili等"

## 设备实测证据（定位根因的唯一依据）

```
target_app       -> 87      （accessibility_enabled=1 亦 87）
skip_rule        -> 0       ← 空
domain_rule      -> 66      （AD 43 / ANALYTICS 13 / CUSTOM 6 / TRACKER 4）
intercept_log    -> 0
```

硬件/系统层**全部正常**，已逐项排除：
- `accessibility_enabled=1`
- `enabled_accessibility_services` 已含 NoAd
- `dumpsys accessibility` 显示
  `Bound services:{Service[label=NoAd 广告拦截, ...]}`
- `canPerformGestures="true"`

## 根因传导链

```
skip_rule 恒空
  → S1RuleCache.buildSnapshot 因 rules.isEmpty() 返回 RuleSnapshot.EMPTY
  → rulesForPackage() 恒 emptyList()
  → EventProcessor 第 1 道闸丢弃 100% 事件
  → 无跳过、无日志、统计恒 0
```

## ⭐ 代码迷思（本次最大教训）

`EventProcessor` / `S1RuleCache` / `UiMatcher` / `ClickExecutor` / `AntiMisclickGate`
**全部实现正确且有单测覆盖**，但它们构成**一条没有入口的流水线**：

- `RuleRepository.add()` / `addAll()` 全项目**零调用点**
- `assets/` 下**无 S1 规则文件**（只有 `builtin_domains.json`，那是 S2 域名规则）
- `feature/` 下**无规则页**

→ 这是「**单测全绿但功能完全不可用**」的典型形态：
**每个零件都对，装配图缺了一根轴。**

## 修复项（R1–R5）

| 编号 | 内容 | 关键文件 |
|---|---|---|
| R1 | 内置跳过规则资产 + 导入管线 + 启动合并 | `assets/rules/builtin_skip_rules.json`、`BuiltinSkipRulesLoader.kt`、`RuleRepository.mergeBuiltin`、`NoAdApplication.initializeSkipRules` |
| R2 | 跳过规则管理页 | `feature/rules/RulesViewModel.kt`、`RulesScreen.kt` |
| R3 | 失败原因细分（消除静默） | `service/event/EventProcessor.kt` 新增 `SkipReason` |
| R4 | 两个加载器共用资产读取 | `core/data/rules/BuiltinRuleAssets.kt` |
| R5 | DB v1 → v2 迁移（新增 `source` 列） | `core/database/Migrations.kt` |

### R1 数据规模

`builtin_skip_rules.json`：**14 个应用 / 20 条规则**，version 1。
覆盖：淘宝(2)、京东(2)、哔哩哔哩(3)、微信(1)、抖音(2)、微博(2)、小红书(1)、
网易云音乐(1)、知乎(1)、美团(1)、腾讯视频(1)、爱奇艺(1)、优酷(1)、UC浏览器(1)。

### R3 的 `SkipReason` 对照表（排查用）

| 值 | 含义 | 用户该做什么 |
|---|---|---|
| `APP_NOT_MANAGED` | 应用未纳管 | 去应用管理页勾选 |
| `NO_RULE_FOR_PACKAGE` | 该应用无规则 | 等规则库扩充或自建规则 |
| `ACTIVITY_MISMATCH` | 界面不匹配 | 规则 activity 限定过窄 |
| `IRRELEVANT_EVENT` | 事件类型无关 | 正常 |
| `NO_ROOT_NODE` / `EMPTY_NODE_TREE` | 取不到节点树 | ROM 限制 |
| `NO_NODE_MATCH` | 未命中任何节点 | 规则定位值需更新 |

日志可见 `事件跳过: <REASON>（<中文说明>）`。
`recordSkipReason` 做了**同值去重**，避免刷屏。

### R5 迁移的关键论证

`Migrations.MIGRATION_1_2` 的 `DEFAULT` **必须是 `'user'`**：

- v1 **没有任何内置规则导入路径**（`RuleRepository.add/addAll` 零调用点，
  assets 中亦无 S1 规则文件）→ 存量行**必然**是用户手工产生的
- 若默认 `'builtin'`，这些用户规则会在**首次导入的替换步骤中被删除**

## ⭐ 三个必须记住的设计决策

### 1. `SkipRuleSource` 必须存 `persistedName`（小写），不能用 `enum.name`

```kotlin
enum class SkipRuleSource(val persistedName: String) {
    BUILTIN("builtin"), IMPORTED("imported"), USER("user");
    companion object {
        // 未知值退回 USER（最需保护）
        fun fromName(raw: String?): SkipRuleSource =
            when (raw?.trim()?.lowercase()) {
                NAME_BUILTIN -> BUILTIN; NAME_IMPORTED -> IMPORTED; else -> USER
            }
    }
}
```

理由：迁移的 `DEFAULT 'user'` 与 `SkipRuleEntity.SOURCE_*` 都是**小写**。
用 `enum.name` 会得到 `"BUILTIN"`/`"USER"`，导致 `WHERE source = 'user'` 查不到新数据，
**现象是「规则莫名消失」**。

**约束**：enum 构造参数**不能引用 companion 常量**
（`Companion object of enum class ... is uninitialized here`），必须写字面量。

### 2. 内置导入必须跨来源去重、且只增不改不删

`RuleRepository.mergeBuiltin` 的四条契约（全部有单测锁住）：

- **幂等**：重复启动/重复导入不产生重复规则
- **只增不删**：不删除任何存量规则
- **不改**：用户对规则的改名/停用/删除**不被覆盖**
- **跨来源去重**：`allExistingKeys()` 查 `dao.loadAll()` 全表，
  用户已建同定位值规则时**阻止内置插入**（否则出现重复项同时参与匹配与点击）

`insertAll` 由 `REPLACE` 改 **`IGNORE`** ——
`REPLACE` 会**重置用户停用状态**并**改变行 id**。

业务键 `SkipRuleKey`：`packageName + activityName + targetType + targetValue.lowercase()`，
**不含** `name` / `priority`（因 `UiMatcher` 匹配本身 `ignoreCase`）。

### 3. CONTAINS 误点防护放**规则编写层**，不放 `UiMatcher`

我曾尝试在 `UiMatcher.matchesContains` 加 8 字符候选长度护栏，
**导致既有测试失败**：`given contains rule when node text contains target then matched`
用 12 字正文 "跳过广告，立即观看正片" 断言应命中。

**我判断该既有测试是正确的**（CONTAINS 就该如其名所示地匹配），
因此**完全撤回**护栏实现，把理由写入注释：

> 护栏放匹配器会破坏规则语义，规则页预览会与运行时不一致。

误点防护改由**两层**承担：
1. **规则编写约束** —— `BuiltinSkipRulesAssetTest` 断言
   ①CONTAINS 目标串 ≥ 4 字符 ②不含诱导性词汇（立即/领取/查看/下载/打开/安装/
   购买/下单/抽奖/红包/优惠/开通/授权/同意/允许）
2. **`AntiMisclickGate` 冷却层**（规则 3s / 节点 5s / 全局 400ms）

## 防回归契约（全部在单测中锁住）

- 重复启动/重复导入不产生重复规则（幂等）
- 用户停用的内置规则不被重新导入复活
- 用户对现有规则的改名/修改不被覆盖
- 合并只增不删
- 跨来源去重（用户规则阻止内置插入）
- 内置规则文件不含诱导性词汇、CONTAINS 目标串 ≥ 4 字符、无坐标规则
- 内置规则对「正常界面」零假阳性
- v1→v2 迁移保留存量数据与停用状态

## 踩坑记录（本次新发现）

### 序列化依赖冲突（真机 `androidTest`）

`room-testing` 引入的 `room-migration` 需要 kotlinx-serialization **1.8.1**
解析 schema JSON，但 AGP 的 **"consistent resolution"** 把主配置中被钉住的
**1.7.3** 以 `strictly` 形式传播到 `debugAndroidTestRuntimeClasspath`。
1.7.3 的序列化运行时与 Kotlin 2.2.10 生成代码 **ABI 不兼容**。

现象：`AbstractMethodError: GeneratedSerializer.typeParametersSerializers()`
—— **错误发生在测试框架内部的描述符哈希计算，报错位置与真实原因完全无关。**

修复：`app/build.gradle.kts` 加
`configurations.configureEach { resolutionStrategy { force(...) } }`
+ `libs.versions.toml` 增 `serialization = "1.8.1"`。
已用 `dependencyInsight` 验证对齐到 1.8.1。

### `MigrationTest` 自身的两个 bug

1. 对空表断言 `cursor.moveToFirst()` 为真
2. `PRAGMA table_info` 的列名是**全小写** `notnull`，我写成 `notNull`
   → `IllegalArgumentException: column 'notNull' does not exist`

修正后改用 `PRAGMA table_info` 逐行查找 `source` 列，
断言 `dflt_value == "'user'"` + `notnull == 1`；
另补 `migrate1To2_appliesUserSourceToRowsInsertedAfterMigration`
（仅查 PRAGMA 只证明 DDL 写对，**不证明插入时生效**）。

### 用户明确叫停实机测试（最重要的边界）

> "我不需要你做实机测试或者编译完整apk文件，所有实机测试由我做，
> 你对实机测试适应太差报错太多，写好代码编译无问题即可"

→ 交付验证标准收敛为：
`./gradlew testDebugUnitTest`（纯 JVM）+ `compileDebugKotlin`（0 error 0 warning）。
**不再**运行 `connectedDebugAndroidTest`，**不再**把 `assembleDebug` 作为交付条件。

## 验证证据（本次有效）

```
./gradlew testDebugUnitTest  → BUILD SUCCESSFUL
  147 tests, 0 failures, 0 errors, 0 skipped
  ├── ExampleUnitTest                1
  ├── RuleRepositoryTest            16  【本次新增】
  ├── BuiltinRulesAssetTest         13
  ├── BuiltinRulesLoaderTest        16
  ├── BuiltinSkipRulesAssetTest     18  【本次新增】
  ├── BuiltinSkipRulesLoaderTest    31  【本次新增】
  ├── DomainRuleEngineTest          15
  ├── AntiMisclickGateTest          13
  └── UiMatcherTest                 24

./gradlew clean testDebugUnitTest assembleDebug → BUILD SUCCESSFUL in 16s
  编译告警: 0（app/src 下无任何 ^w:）
  来源编码一致性已验证：
    SkipRuleEntity.SOURCE_*  = "builtin"/"imported"/"user"
    SkipRuleSource.BUILTIN("builtin")
    Migrations DEFAULT 'user'
```

## 文档诚实性修正

阶段 B 原先标的「✅ 已完成」是**过度声明**（当时只有编译与单测证据）。
已改为「⚠️ 代码完成，待实机验证」，并在 `THREE_STRATEGY_PLAN.md`
新增完整「阶段 B 勘误与修复」章节。

**三条教训**（已写入验收清单）：
1. **链路连通性必须有测试** —— 零件全绿 ≠ 链路可用
2. **「已完成」必须附实机证据**
3. **启动路径上的导入必须双向检查**（不只是"有规则"，而是"规则能被读到"）

---

# 第三轮：S1 规则与真实节点不符（R6）

## 用户报告

> "拦截仍然全部为0，实际仍然有开屏广告，bilibili 美团 美图外卖等等"

上一轮（R1–R5）修复后**实机仍然无效**。

## 真机取证：入库通了，匹配没通

```
skip_rule       = 20   （source='builtin'）
domain_rule     = 66
target_app      = 88
intercept_log   = 0    ← 仍为 0
```

→ R1/R5 的入库部分成功（规则确实进库、迁移正确），
但 `intercept_log` 仍为 0，**卡在匹配阶段**。

## 根因：规则是凭常识捏造的

真机抓包 B 站开屏节点：
```
rid  = tv.danmaku.bili:id/count_down
text = "跳过 1"          ← 带倒计时
clickable = true
```

而我写的三条 B 站规则**全部落空**：
| 规则 | 定位值 | 结果 |
|---|---|---|
| viewId | `tv.danmaku.bili:id/skip` | ❌ id 不存在（凭空捏造） |
| TEXT EXACT | `跳过广告` | ❌ 实际是「跳过 1」 |
| TEXT EXACT | `跳过` | ❌ 实际是「跳过 1」 |

→ `NO_NODE_MATCH`。SKIP 项目配置里 B 站同样是 `count_down`，互相印证。

## 用户指定方向

> "前缀匹配+严格护栏，同时我需要你去网络搜索其他广告拦截方案的规则配置，
> 不要局限于我的单台机器测试，汇总一下网上的方案"
> "规则页显示实时匹配诊断（推荐）"

## 竞品调研（新增 `reference/SKIP_RULE_RESEARCH.md`）

| 方案 | 做法 | 启示 |
|---|---|---|
| GKD | `[text*="跳过"][text.length<10][visibleToUser=true]` | 「包含+长度上限+可见性」三重约束是社区共识 |
| 李跳跳 | `keywords` 模糊匹配，`+` 前缀修饰符 | 前缀匹配有成熟先例 |
| SKIP | B站用 `count_down`；大量复用 SDK 统一 id | 印证 `count_down` |
| 穿山甲 SDK | `tt_splash_skip_btn`（跨应用一致） | **一条规则覆盖所有接入方** |
| 快手 SDK | `ksad_splash_circle_skip_view` | 同上 |

共同点：开屏按钮**几乎都带倒计时**；各方案**都保留文本兜底**；`activityIds` 普遍不写。

## 代码改动

### 新增
- `MatchMode.PREFIX`（前缀 + 长度上限 10，对应 GKD 的 `[text.length<10]`）
- viewId **后缀匹配**：伪前缀 `*tt_splash_skip_btn` 匹配任意宿主包名
- **通用规则层**：伪包名 `GLOBAL_RULE_PACKAGE = "*"`
- `SkipDiagnostics`（内存 StateFlow 实时诊断，绕开被 ROM 抑制的 logcat）
- 规则页顶部「匹配诊断」卡片 UI
- `S1RuleCacheTest`（15 个用例）、`SkipDiagnosticsTest`（14 个用例）

### 规则库重写（version 1 → 2）
三层：通用层（4 条）→ 应用专属 viewId → 应用专属文本兜底。
**15 个应用组 / 21 条**。
删除：微信整组、全部 5 条 `关闭广告` PREFIX 规则。

### 关键重构
`S1RuleCache` 主构造函数改为接受**两个 Flow** 而非两个仓库 ——
`TargetAppRepository` 需要 `Context`，而项目测试栈无 Robolectric，
原签名会让快照构建逻辑（含核心安全契约）**永远无法在 JVM 上验证**。

## 🔴 测试抓出的真实安全漏洞（最重要的一条）

`S1RuleCache.rulesForPackage` 首版：
```kotlin
if (global.isEmpty()) return own ?: emptyList()
if (own.isNullOrEmpty()) return global   // ← 缺陷
```
**未纳管应用**既不在 `byPackage`（`own` 为 null），
又因通用规则存在而入围第二个分支 → **拿到了本不该生效的通用规则**。
后果：用户只勾选了 B 站，淘宝开屏时通用规则命中，
**应用在用户从未授权的应用上执行了点击**。

修正：**先判 `isManaged`**，通用规则同样受"纳管即授权"约束。
由 `S1RuleCacheTest.givenGlobalRuleOnly_whenRulesForUnmanagedPackage_thenEmpty` 钉死。

> 这个漏洞是我自己在写测试时被逼出来的 —— 如果只跑实机，
> 现象是"某些没勾的应用也被点了"，极难归因。

## 其它由测试捕获的缺陷（3 个）

1. **PREFIX 单独用不足以防正文** —— 我曾断言"正文不会以『跳过』开头"，
   但测试树里的 `跳过此步可在设置中重新开启` **正是反例**。
   修正：加 `MAX_PREFIX_CANDIDATE_LENGTH = 10`。
2. **`关闭广告` PREFIX 仍有假阳性** —— `关闭广告推送通知` 仅 8 字未超上限。
   修正：删除全部 5 条。
3. **`desc="关闭"` 缺 activity 限定** —— 修正：加测试强制
   `DESCRIPTION + EXACT/CONTAINS` 必须有 `activityName`。

## 验证证据（R6）

```
./gradlew testDebugUnitTest --rerun-tasks  → BUILD SUCCESSFUL
  182 tests completed, 0 failed, 0 skipped
./gradlew compileDebugKotlin               → 0 error, 0 warning（无任何 ^w:）
```

按用户界定标准：**不跑实机、不打 APK**。

## R6 四条教训

1. **规则必须来自实证，不能来自常识** ——
   "某应用大概有个叫 skip 的按钮"这类推断一次都不该出现。
2. **竞品调研是规则库的建设手段，不是可选项** ——
   单机抓包覆盖有限且开屏时机难捕捉，社区规则库与 SDK 文档才可规模化。
3. **测试是发现设计缺陷的有效工具，不只是防回归** ——
   本轮 3 个缺陷全由测试捕获，加上 1 个安全漏洞。
4. **通用规则层是必需品** —— 但必须配合"纳管即授权"的安全闸门。

---

# R7（2026-09-19）—— 启动应用半秒延迟

## 用户报告

> "广告拦截关闭成功，但是启动任何应用时存在一个半秒左右的明显延迟"

**注意反馈的性质变化**：这不是"拦截失效"，而是**拦截生效后引入的副作用**。
排查方向完全不同于 R6 —— 这次要找的是"谁在主线程干了重活"。

## 根因（三个叠加来源）

1. **点击在主线程同步执行**。`onAccessibilityEvent` 默认在主线程回调，
   而 `ClickExecutor.execute` 内含 `findByIndex` 全树 BFS 回查 +
   `ACTION_CLICK`/`getParent` IPC，失败时回落 `dispatchGesture`（手势播放约 40ms）。
   单次 50–300ms。
2. **每个相关事件都遍历整树**。已纳管应用启动时内容变化事件数十次，
   每次 `rootProvider()` + `UiTreeScanner.scan()`；500 节点 = 数百次 `getChild` IPC。
3. **`notificationTimeout = 100ms`** 额外引入最多 100ms 延迟。

三者叠加正好是用户感知的"半秒"。

## 修复四项

1. **`EventPreFilter`（新增）** —— 用事件自带 text/desc 做无 IPC 预筛，
   不沾边直接 `Ignored(PRE_FILTERED)` 返回，一次节点读都不做。
2. **点击投递到 `Dispatchers.Default`** —— `performAction`/`getChild` 是 IPC，
   **不受"必须主线程"约束**。新增 `ProcessOutcome.ClickScheduled` / `Deferred`。
3. **`EVENT_TIMEOUT_MS` 100 → 0** —— 及时性换回来，降频职责交给预筛。
4. **Room 启用 WAL** —— `.setJournalMode(WRITE_AHEAD_LOGGING)`。

## ⭐ 核心铁律（R7 最重要的产出）

**预筛只放宽、不收紧。**

预筛一旦比权威实现 `UiMatcher` 严格，就产生"事件被预筛丢弃、永远走不到匹配"的
**静默漏拦**，现象与"规则写错"完全一致，极难排查。

具体推导出的规则：
- 规则集为空 → 放行
- **任何 `VIEW_ID`/`COORDINATE` 型规则 → 整体放行**。
  原因：`AccessibilityEvent` **没有** `viewIdResourceName` 属性
  （那是 `AccessibilityNodeInfo` 才有的），从事件侧根本无法判断 viewId 规则。
  这一条是我写代码时踩出来的真坑（编译期报 `Unresolved reference`）。
- 任何 `REGEX` 型规则 → 整体放行（正则 ≠ 前缀匹配）
- 事件无文本候选 → 放行（信息不足绝不能替权威实现做否决）
- **不做 activity 预筛** —— 纯字符串比较无 IPC 成本，交给 `filterByActivity` 即可

由 `EventPreFilterTest.givenRealWorldSkipTexts_whenMatcherHits_thenPreFilterAlwaysPasses`
强制守护：枚举真实文案，逐条断言「`UiMatcher` 命中 ⇒ 预筛必然放行」。

## 由测试捕获的设计缺陷

我在 `EventPreFilter` 里自实现了 activity 预筛，**它比权威实现更严格**
（class 不同就过滤掉）→ 违反"只放宽"不变式。
测试失败两次才暴露：第一次我误把失败归因于 activity，用"无关内容"当候选；
第二次移除 activity 预筛后仍失败，才发现断言本身有问题。
改用 `"跳过 1"` 使文本匹配成立，才真正隔离出要验证的点。

> **这条是 R7 最有价值的产出 —— 它是测试抓出来的，不是实机抓出来的。**
> 放任不管的话，用户会看到"某些应用又拦不住了"，
> 而排查方向会错误地指向规则文件。

## 诊断扩展（供用户实机核对）

`SkipDiagnosticsState` 新增 `lastCostMs` / `maxCostMs` / `slowEventCount`，
常量 `FRAME_BUDGET_MS = 16L`、`SLOW_EVENT_THRESHOLD_MS = 100L`，派生 `hasFrameDrop`。
规则页诊断卡片新增「单次处理耗时：最近 Nms · 峰值 Mms」。

**`ClickScheduled` 不计入 `totalClicks`** —— 投递 ≠ 点击成功，统计口径不能乐观化。

## 验证证据（R7）

```
./gradlew testDebugUnitTest compileDebugKotlin    → BUILD SUCCESSFUL
  210 tests / 12 test classes, 0 failed, 0 errors
./gradlew clean compileDebugKotlin --rerun-tasks  → 0 error, 0 warning
```

新增测试：`EventPreFilterTest` 17 个、`SkipDiagnosticsTest` 新增 10 个。

按用户界定标准：**不跑实机、不打 APK**。

---

# R8（2026-09-19）—— 无障碍"失效"的自愈

## 用户请求（前提需要纠正）

> "无障碍权限在离开应用界面一段时间或息屏或切换应用等情况之后系统自动又关闭了，
> 想办法使无障碍权限开启后在手机未关机情况下常驻"

**前提错了，必须先纠正方向**：

**无障碍不存在"常驻"这个状态，因此没有"让它常驻"的实现方式。**
Android 的无障碍是**授权模型**——用户勾选后系统写入
`Settings.Secure.ENABLED_ACCESSIBILITY_SERVICES`，之后**由系统自行决定**
何时连接、何时解绑。**没有任何 API 能让应用要求系统"保持连接"。**

官方 `AccessibilityServiceInfo` 文档对 flags 的原文印证：
> This field represents a set of flags used for **configuring** an AccessibilityService.

关键推论：**服务被解绑时，授权记录依然存在** —— 这正是它能自动重连的原因。

| 用户感知 | 实际状态 | 结论 |
|---|---|---|
| "权限被自动关了" | 授权还在，只是服务实例被解绑 | **不需要重新授权** |
| 真正要解决的 | 断开未被及时发现与呈现 | 做**自愈检测**，不是保活 |

## 明确不做（三条路，都有理由）

1. **前台服务 / AlarmManager / JobScheduler 保活** ——
   对无障碍**完全无效**。它们影响进程优先级，而服务绑定由系统的
   无障碍管理器决定。白耗电，且与 `NoAdAccessibilityService` 原有注释冲突。
2. **adb / Shizuku 写 Settings.Secure** —— 需用户每 12 小时重新授权调试；
   属系统性写操作，影响面超出本应用。
3. **FLAG_REQUEST_ACCESSIBILITY_BUTTON** —— 用户选定"只做常规自愈"。
   且官方明确：`targetSdk > 29` 时运行时设置该 flag **会被忽略**，
   必须写进 XML；部分 ROM 不提供该按钮。

## 用户的三项决策

- 恢复方式 → **只做常规自愈（推荐）**
- 状态可见性 → **如实展示断开，并给出原因（推荐）**
- 触发时机 → **应用回到前台 + 息屏/解锁**（未选开机、网络变化）

## 四项改动

1. **状态模型**：`AccessibilityState` 新增 `lastDisconnectedAtMillis` /
   `disconnectReason`，以及三态区分：
   ```kotlin
   val isDisconnectedButAuthorized: Boolean   // ⭐ 已授权但未连接（可自愈）
       get() = !serviceRunning && serviceEnabledInSettings
   val isNotAuthorized: Boolean
       get() = !serviceRunning && !serviceEnabledInSettings
   ```
   新增 `DisconnectReason` 枚举（SYSTEM_UNBOUND / SERVICE_DESTROYED /
   USER_DISABLED_IN_SETTINGS / UNKNOWN）。
   **诚实性约束**：Android 不提供"解绑原因" API，因此**不做推测**，
   只区分确实观察到的回调路径，其余归 UNKNOWN。

2. **首因优先**：`onUnbind` 与 `onDestroy` 因同一次断开先后触发，
   前者信息更具体。`onDisconnected` 不接受覆盖：
   ```kotlin
   disconnectReason = current.disconnectReason ?: reason   // 不是 reason ?: current
   ```
   断开时刻同理只在**首次**写入。

3. **`AccessibilityWatchdog`**（新增，进程级）——
   SCREEN_ON / USER_PRESENT / BOOT_COMPLETED / ACCESSIBILITY_STATE_CHANGED(API 31+)。
   都是系统广播，不受 Android 8+ 后台限制。在 `Application.onCreate`
   **同步启动**（不能进异步块——广播可能早于初始化完成）。
   Android 14+ 须传 `RECEIVER_NOT_EXPORTED`。
   **刻意不注册 SCREEN_OFF**：息屏瞬间状态无意义。

4. **两层刷新分工 + UI 如实呈现** —— 后台靠 Watchdog 广播；
   前台靠 `HomeScreen` 的 ON_RESUME（覆盖"从设置页返回"，
   此时屏幕既没亮也没解锁、广播不会发）。**两层缺一不可。**
   新增 `DisconnectNotice`：**刻意不用 error 配色**、主按钮是
   "前往设置检查"而非"去开启"，文案明确写"不需要重新授权"。

## ⭐ 由测试捕获的两个缺陷

1. **`onDisconnected` 写成 `reason ?: current`** ——
   等价于"有值就覆盖"，`onDestroy` 依然会盖掉 `onUnbind` 的更具体原因。
   由 `givenAlreadyUnbound_whenOnDestroyCalled_thenDoesNotOverwriteReason` 钉死。

2. **`AccessibilityStateHolder` 直接用 `SystemClock.elapsedRealtime()`，
   导致整个状态机在 JVM 上无法测试** ——
   `android.jar` 是空壳，方法体全是 `throw`，报
   `Method elapsedRealtime in android.os.SystemClock not mocked`。
   **而状态机决定 UI 说"去设置"还是"等一等"，是最不能靠猜的部分**，
   它在修改前完全不可测。
   修正：抽出 `internal var clock: () -> Long = { SystemClock.elapsedRealtime() }`，
   测试注入计数器 → 「已断开多久」的边界
   （刚断开 / 59秒 / 1分 / 59分 / 1小时）才变得可验证。

> 第 2 条是 R8 最有价值的产出。**也是测试逼出来的，不是读代码发现的。**

## 顺带修掉的 UI 逻辑漏洞

`when` 分支里，`isDisconnectedButAuthorized && appSwitchEnabled` 之前会落到
`else`，给用户显示「开启无障碍拦截」——**而它本来就是开着的**。
会误导用户去点一个无意义的按钮。已补显式分支。

同时把硬编码的 `Text("已开启")` 换成 string resource（违反项目规范）。

## 测试的诚实边界

`refreshFromSystemSettings` 依赖 `ContentResolver`，项目**无 Robolectric**，
JVM 拿不到。因此提供 `internal fun seedSettingsFlagForTest(enabled: Boolean)`
验证**由该字段驱动的三态判定**。

**不覆盖**设置读取路径本身——那是 Android 平台行为，由用户实机验证。
已在测试类 KDoc 中如实标注，**不假装覆盖了 `Settings.Secure` 读取**。

## 验证证据（R8）

```
./gradlew testDebugUnitTest compileDebugKotlin    → BUILD SUCCESSFUL
  231 tests / 13 test classes, 0 failed, 0 errors
  （新增 AccessibilityStateHolderTest 21 个）
./gradlew clean compileDebugKotlin --rerun-tasks  → 0 error, 0 warning
```

按用户界定标准：**不跑实机、不打 APK**。

---

# R9（2026-09-19）—— 后台保活：进程存活与自恢复

## 用户请求

> "现在实现一下软件本身的后台常驻避免被意外杀掉，以及软件自拉起处理
> 在非关机情况下被杀掉后需要自启的情况，这个功能需要在设置界面配置可关闭"

## 与 R8 的分工（关键前提）

R8 管**无障碍服务连接层**（系统解绑 → 重连，授权模型内自愈）；
R9 管**应用进程存活层**（进程被杀 → 拉起）。两层互补不冲突：
连接层自愈的前提是进程活着；进程死了必须由进程外机制拉起。
R8 文档中「保活对无障碍无效」的结论依然成立——R9 的保活
**不是**为了让无障碍绑定不断开，而是防止进程死亡这种更彻底的失效。

## 用户决策（RESOLVE 确认）

- 开关结构 → **两个独立开关**：「后台保活」（新）+「开机自启动」（假开关接真）
- 电池优化 → **加豁免直达入口**

## 核心设计：三层恢复链

| 层 | 触发点 | 延迟 | 原理 |
|---|---|---|---|
| ① 粘性重启 | START_STICKY 被杀后系统自行重启 | 秒级 | 系统发起的重启豁免后台启动限制，onStartCommand 收 null intent |
| ② 心跳闹钟 | 15 分钟一次性闹钟链（setAndAllowWhileIdle 非精确） | ≤15 分钟 | PendingIntent 系统侧持有，**进程死亡不清除闹钟** |
| ③ 系统广播 | BOOT_COMPLETED + MY_PACKAGE_REPLACED | 重启/更新后 | 官方 FGS 后台启动豁免；更新清空闹钟后由它重建链条 |

关键决策：
- **心跳失败也必须重排**（TRY_START_AND_RESCHEDULE 字面契约）——
  一次性闹钟不重排即断链，失败 → 下次心跳重试是唯一自愈路径
- **镜像乐观策略**：粘性重启时镜像 null（未同步）→ 乐观恢复；
  唯一硬约束是明确 false 必须停止（「关不掉」bug 的唯一入口）
- **接收器读 DataStore 权威值**而非内存镜像（BOOT 路径进程刚拉起，
  镜像必为 null，读权威值避免启动竞态）
- **非精确闹钟**（setAndAllowWhileIdle）：精确闹钟 13+ 需
  SCHEDULE_EXACT_ALARM 且默认被拒；15 分钟 > Doze 9 分钟合并窗口
- **ELAPSED_REALTIME_WAKEUP** 单调时钟：不受改时间/时区影响
- **闹钟 PendingIntent 用显式 Intent**，不进 manifest filter，无第三方触发面
- **刻意不在 Service.onDestroy 取消闹钟**（心跳必须比服务活得久）
- **FGS 类型用 specialUse**：Android 15 BOOT 限制只放行 6 类 FGS，
  specialUse 在白名单内；manifest 带 PROPERTY_SPECIAL_USE_FGS_SUBTYPE
- **通知渠道 IMPORTANCE_LOW**：常驻通知静默；13+ 通知权限拒绝
  不阻断保活（服务照常跑，仅通知不可见）

## 双开关门控语义

- 保活开关：管「运行期不被杀 + 被杀自恢复」（粘性重启/心跳/更新后恢复只看它）
- 自启开关：只管「重启后恢复保活」；BOOT 门控 = `keepAlive && autostart`
- **更新后恢复（MY_PACKAGE_REPLACED）只看保活开关**：更新是用户主动行为
- 设置页副标题约束：自启开关写「设备重启后恢复后台保活」，
  **不能写「恢复拦截」**（无障碍重启后由系统授权模型自动重连，与开关无关）

## 诚实边界

**force-stop 后任何应用都无法自启**（Android 安全设计），三层恢复链
覆盖的是系统回收场景，文档与 UI 均不夸大。

## 新增/修改文件

新增（`core/service/keepalive/`）：
- `KeepAlivePolicy.kt` —— 纯决策层（internal object，JVM 可测），
  四个决策函数 + 四个枚举，锁死两条不变式
- `KeepAliveRuntime.kt` —— 设置三态镜像（@Volatile，null=未同步）
- `KeepAliveScheduler.kt` —— 闹钟排期/取消，`HEARTBEAT_INTERVAL_MS = 15 * 60_000L`
- `KeepAliveService.kt` —— specialUse 前台服务；companion `ensureStarted()`
  吞 `ForegroundServiceStartNotAllowedException`（等心跳重试）
- `KeepAliveReceiver.kt` —— BOOT/更新/心跳三分发，goAsync() + IO 协程
- `res/drawable/ic_notification.xml` —— 白色盾牌（通知小图标须纯 alpha）

修改：
- `SettingsRepository.kt` —— `keepAliveEnabled`（默认 true）+ `autostart`
  （独立 Flow，默认 false）
- `NoAdApplication.kt` —— 单一接线观察者：设置变化 → 更新镜像 + 启停服务
- `AndroidManifest.xml` —— REQUEST_IGNORE_BATTERY_OPTIMIZATIONS 权限；
  KeepAliveService（specialUse + SUBTYPE property）；KeepAliveReceiver
  （exported=true，filter 仅 BOOT_COMPLETED + MY_PACKAGE_REPLACED，均为受保护广播）
- `strings.xml` —— R9 全套字符串；`SettingsViewModel/Screen` —— 保活开关、
  电池豁免行（PowerManager 状态 + ON_RESUME 刷新 + 精准对话框 intent，
  失败退回列表页）、通知权限请求（SettingsRoute 作用域 lambda，
  解决「普通 lambda 内不能调 @Composable」）

## 本轮踩坑

1. **`SettingToggleRow.onToggle` 是 `() -> Unit` 无参回调**——首版写成
   `{ enabled -> ... }` 编译失败。修正：回调内 `!当前值` 取反（与其他行一致）。
2. **通知权限请求不能写成 @Composable 函数**——调用点在非组合作用域的
   onToggle lambda 内。修正：在 SettingsRoute 作用域 `rememberLauncherForActivityResult`
   后暴露为 `val requestNotificationPermissionIfNeeded: () -> Unit` lambda 属性。

## 验证证据（R9）

```
./gradlew :app:compileDebugKotlin → BUILD SUCCESSFUL, 0 error, 0 warning
./gradlew :app:testDebugUnitTest  → BUILD SUCCESSFUL
243 tests / 14 test classes, 0 skipped, 0 failed, 0 errors
  （新增 KeepAlivePolicyTest 12 个：粘性三态/BOOT 四组合/更新两态/
    心跳两态/Doze 窗口不变式；报告 XML 聚合核验）
```

文档同步：`THREE_STRATEGY_PLAN.md` 新增「阶段 B 功能扩展（R9）」章节；
README 新增保活使用说明与 force-stop 诚实边界。

按用户界定标准：**不跑实机、不打 APK**。

---

# R10（2026-09-19）—— 最近任务隐藏 + 常驻拦截动态通知

## 用户请求

> "1、本软件在上划后台应用中不显示；2、在下拉通知栏中常驻显示拦截
> 数量和上一条拦截的是什么，仅在通知栏显示不需要通知横幅"

## 需求 1：excludeFromRecents

`MainActivity` 加 `android:excludeFromRecents="true"`（清单静态声明）：
隐藏任务卡片但任务照常存在（桌面图标可回原界面）；无法从最近任务
划掉/一键清理 = 与保活互补。刻意不做运行时切换。

## 需求 2：常驻通知实时拦截动态（无横幅）

升级 R9 保活 FGS 的常驻通知内容（通知本身是系统要求、不可移除）：

- 标题「已拦截 N 次广告」+ 正文「最近拦截：app · 类型」
- 数据链：`InterceptLogDao` 新增 `observeTotalCount()`/`observeLatest()`
  （LIMIT 1）→ `LogRepository` 暴露 Flow → 服务三路 combine → notify 同 ID 覆盖
- **无横幅是结构性的**：R9 既定 `IMPORTANCE_LOW` 渠道 + 通知更新
  在低优先级渠道永不触发 heads-up，不依赖补丁式配置
- `InterceptLog` 已冗余存中文 `appLabel` 与 `adType.label`，无需解析包名

关键决策：
1. **计数以表为准，不做独立累加器** —— 与日志页单一数据源
   （clear 同步归零；裁剪后计数回落换取两数字永远一致）
2. **接活死置的 `showNotification` 开关**（原「拦截时发送通知提醒」
   无消费方）→ 语义改为「常驻通知是否显示拦截动态」，关闭回退静态文案
3. **首帧静态文案**：startForeground 同步完成，观察者流毫秒级后刷新
4. Android 13+ 无通知权限跳过投递（不崩溃，FGS 任务管理区仍可见）
5. `KeepAliveNotificationContent` 纯决策层锁四分支；两个防御分支：
   计数>0 但最新记录缺失（COUNT 与 LIMIT 1 是两次独立快照，裁剪/
   并发交错可出现）→ 正文回退「暂无」；应用名空串 → 兜底「未知应用」
   （`ResId` value class 区分资源参数与 Int 次数参数）

## 修改文件

- `AndroidManifest.xml` —— excludeFromRecents
- `InterceptLogDao.kt` —— observeTotalCount/observeLatest
- `LogRepository.kt` —— 对应 Flow
- `SettingsRepository.kt` —— `showNotification: Flow<Boolean>`
- `KeepAliveService.kt` —— 服务级 scope（onDestroy 取消）+ 三路 combine
  观察者 + canPostNotifications 守卫 + buildNotification(content) 参数化
- `KeepAliveNotificationContent.kt`（新）—— 纯决策层 + ResId
- `strings.xml` —— 状态文案 4 条 + 开关行 2 条
- `SettingsScreen.kt` —— 拦截通知行改资源引用并更新语义
- `KeepAliveNotificationContentTest.kt`（新，6 用例）

## 踩坑

1. **`@StringRes` 在构造参数上触发 KT-73255 弃用路径警告** ——
   Kotlin 对「注解默认同时作用于 param 与 field」的迁移期告警。
   修：`@param:StringRes` 显式目标（编译器自提示）。
2. **`ResId` 引用写错层级**（`KeepAliveNotificationContent.ResId` vs
   顶层 `ResId`）—— 测试编译失败一次，改顶层引用。

## 验证证据（R10）

```
./gradlew :app:compileDebugKotlin → BUILD SUCCESSFUL, 0 error, 0 warning
./gradlew :app:testDebugUnitTest  → BUILD SUCCESSFUL
249 tests / 15 test classes, 0 skipped, 0 failed, 0 errors
  （新增 KeepAliveNotificationContentTest 6 个，报告 XML 聚合核验）
```

文档同步：THREE_STRATEGY_PLAN.md R10 章节；README 能力表与使用说明。

实机验证点：最近任务无 NoAd；拦截后通知实时更新且无横幅；
关闭「拦截动态」回退静态文案。

## R11（2026-09-19）：阶段 F 前置 —— F1 Shizuku 基础设施 + F2 侧载限制解除

### 交付范围
- **F1**：shizuku api/provider/aidl 13.1.5 接入（toml + buildFeatures{aidl=true}）+
  Manifest ShizukuProvider；`ShellCommandUserService`（自有 AIDL 单方法 exec，
  10s 超时，先限时等待再读流，绝不抛异常）；`ShizukuShellClient`
  （bindUserService + CompletableDeferred 8s 超时，死亡 reset 重绑）；
  `ShizukuAvailabilityHolder` 状态机 Unavailable/NeedsPermission/Probing/Ready(canSetAppOps)，
  Application 三监听（sticky received/dead/权限结果）驱动 + Mutex 串行刷新。
- **F2**：`RestrictedSettingsOps` 纯决策层（op 名 ACCESS_RESTRICTED_SETTINGS、
  「op名+冒号」严格前缀解析、包级→--uid 变体计划）+ `RestrictedSettingsFixer`
  （get 回读验证 = 成功判定）+ `SideloadRestrictionController` 门面
  （ShizukuSupport 布尔流 + ResolveOutcome 五态；feature 零 shizuku 引用）+
  `AccessibilityStateHolder.markRestrictedSettingCleared()`（进程内覆盖保守判定，
  刻意不持久化）。UI：HomeUiState +shizukuReady/shizukuNeedsPermission/fixingRestricted
  （combine 扩 4 路）；受限卡三形态互斥；成功直接打开系统无障碍设置。
- 测试：`RestrictedSettingsOpsTest`（24：命令构造/输出解析/决策，含 op 数值守护）、
  `AccessibilityStateHolderRestrictedTest`（3）。

### 传输层决策（对方案 §6.5.2 的有意识偏差）
UserService + 自有极简 AIDL + cmd appops 字符串名。理由：官方将移除 newProcess；
框架 AIDL 方法事务码按声明序分配会静默漂移；op 数值跨版本漂移。
aidl 开关 = `buildFeatures { aidl = true }`（AGP 9.x 实测可解析）。

### 事故与修复（重要教训）
- 🔴 **并行同文件编辑覆盖**：同一文件多个 Edit 同批随机互相覆盖
  （toml 版本/imports/字段/函数体各处静默丢失），下游编译错误极具迷惑性。
  修复分 5 轮执行（每文件每轮一个编辑）。教训已写入 skill §十三与 MEMORY.md。
- 编译错误修复清单：`ShizukuState` 密封接口声明在事故中丢失 → 补回（包级声明）；
  `ShellCommandUserService` 缺 Context import；
  internal 暴露（ShellClient/Fixer/Ops 去 internal —— AppContainer 公开属性持有所致）；
  `Shizuku.getServerVersion()` 不存在 → `Shizuku.getVersion()`。

### 验证证据
```
./gradlew testDebugUnitTest compileDebugKotlin → BUILD SUCCESSFUL
276 tests / 17 类 / 0 failures / 0 errors / 0 skipped
compileDebugKotlin 0 error 0 warning（app/src 无 ^w:）
```

文档同步：THREE_STRATEGY_PLAN.md 追加 R11 章节；README 新增
「侧载被『受限设置』挡住？用 Shizuku 一键解除」；MEMORY.md 重写
（276/17 基线 + Shizuku 定稿修正：旧记忆中的 newProcess/Ready(shellExecutable)
与最终实现相悖已纠正；并行编辑教训）；skill noad-dev-standards 新增 §十三。

实机验证点（用户执行）：Android 13+ 侧载设备装 Shizuku → 首页受限卡
「授权 Shizuku」→「自动解除」→ 点击后 toast + 落到无障碍设置且开关可开；
返回首页提示不再出现；杀掉 Shizuku 后回退手动图文（无崩溃无死按钮）。

---

# R12（2026-09-19）—— Shizuku 一键恢复无障碍授权（被「一键清理」清掉后）

## 用户报告与诊断

> "上拉最近菜单点击清理软件后软件就会丢失无障碍权限，必须前往设置页重新开启"

定性：ROM「一键清理/上划清除」按 **force-stop 语义**处理应用 → 系统撤销
`Settings.Secure.ENABLED_ACCESSIBILITY_SERVICES` 中的授权条目。系统行为
**无法阻止，只能事后恢复**（与 R9 force-stop 诚实边界同源）。
排查已排除自伤：Manifest 无 stopWithTask、excludeFromRecents 已声明、
KeepAliveService 无 onTaskRemoved。

## 方案：Shizuku 显式点击恢复（复用 R11 通道，计划 §6.4/F4 已许可）

**R8 禁止表边界修订（已写入 plan）**：当年否决的是「周期性后台自动写授权记录」
（adb 12h / 影响面）；用户显式点击的一次性恢复不在此列；后台静默周期写依然禁止。

## 三层实现

- `AccessibilityRestoreOps`（纯决策层，public object）：**read-merge-write**——
  `enabled_accessibility_services` 是全局共享设置，直接覆盖会抹掉**其他应用**的
  条目。settings get → parseEntries（容忍 "null" 字面量/脏分隔符/空白）→
  mergeEnabledServices（**已存在条目零改写**，含大小写变体，不规范化不追加）→
  put → **再 get 回读验证**（set 不报错 ≠ 生效，SELinux/ROM 静默拒绝是真实
  失败形态）；另写 accessibility_enabled=1。restore(exec) 把命令执行收敛为
  注入函数（生产 shell::exec / 测试 ScriptedExec，与 S1RuleCache 收 Flow 同思路）。
- `AccessibilityAuthorizationRestorer`（薄适配器，public 不能 internal）
- `AccessibilityRecoveryController`（门面五态，与 SideloadRestrictionController
  平行独立——「恢复授权」与「解除受限」是两个关注点，不共名）

接线：NoAdApplication 容器（restorer = AccessibilityAuthorizationRestorer(shell)）
→ NoAdViewModelFactory → HomeViewModel.restoreAccessibilityAuthorization()
（与 resolveRestrictedSettings 共用 fixInFlight 防重入）→ HomeScreen 恢复按钮
（shizukuReady 时 OutlinedButton，fixInFlight 禁用）+ 5 条 strings。

## 测试与踩坑（AccessibilityRestoreOpsTest 31 例，ScriptedExec 按序记录调用）

**4+1 个测试失败全是断言错误，实现正确**：
① merge 对大小写变体**零改写**（保留原样不规范化），测试却断言了规范化；
② write 命令谓词写成 it.first()=="put"（首元素是 "settings"）→ 改 it[3]==key；
③ diagnosticTail("null") = Failed("null") 非 Failed(null)；
④ 空回读 diagnosticTail("") = null → Failed(null)；
⑤ **read 与 write 命令第 4 参都是键名**，.first{} 会匹配到 read ——
改为按序位断言 exec.commands[1]。

工具教训：`--rerun-tasks` 与配置缓存不兼容（2 秒快退、告警校验无效）；
强制重编译正确姿势 = `find app/src/main/java -name "*.kt" -exec touch {} +`
后 compile + `grep "^w: "`。

## 验证证据

```
./gradlew testDebugUnitTest compileDebugKotlin → BUILD SUCCESSFUL
307 tests / 18 类 / 0 failures / 0 errors / 0 skipped
touch 强制重编译 → app/src 0 warning（grep "^w: " 仅 warnings_done）
```

文档同步：plan R12 章节（含 R8 边界修订）；README「授权被清理掉」小节；
skill §十三 R12 小节；MEMORY.md（307/18 基线）。

实机验证点（用户执行）：授权被一键清理清掉后 → 启动 Shizuku → 首页出现
「用 Shizuku 恢复无障碍授权」→ 点击 toast 成功、服务自动重连、无需进系统设置；
未装 Shizuku 时按钮不出现（行为与旧版一致）；恢复失败 toast 引导手动开启。

---

# R13（2026-09-19）—— adb 高级授权：无 Shizuku 的恢复通道（阶段 B 收口）

## 背景与决策

R12 恢复依赖 Shizuku（安装 + 启动 + 重启后重拉 server），对多数用户是
持续负担。查证（Tasker/AutoTools 通行做法 + AutoTools 官方论坛确认）：
`adb shell pm grant <pkg> android.permission.WRITE_SECURE_SETTINGS` 授予的
是 install-time 权限，**一次授权终身有效**（重启不清、清数据不丢，仅
卸载重装失效）。持有后应用可用 ContentResolver 直接读写 Settings.Secure。
附赠收益：直接写设置值不经过设置页 UI 的「受限设置」检查，R11 要解的
Android 13+ 侧载开关拦截问题在此一并覆盖。用户批准后实施。

## 实现（复用 R12 决策层，只换 IO 层）

- **`SecureSettingsAccessibilityRestorer`**（core/service/，ContentResolver
  解释器）：把 `AccessibilityRestoreOps.restore(exec)` 发出的 shell 形命令
  翻译为 getString/putString。关键语义对齐：
  - getString 返回 null（键不存在）→ 映射 `"null"` 字面量 → 决策层与
    shell 看到相同形态（首次恢复不误判失败）
  - SecurityException/IllegalArgumentException（权限被撤）→ 传播到
    restore() 捕获为 Failed(null)，**零写入**
  - 读写为注入函数（主构造），ContentResolver 走次构造 → JVM 全可测
  - put 返回值刻意不检查：与 shell 一致，生效与否由回读验证判定
- **门面双通道优先级**：`hasSecureWritePermission`（注入函数，JVM 可测）
  在位 → 优先 ContentResolver 直写（与 Shizuku 无关）→ 否则回退 Shizuku
  （R12 预检不变）→ 都没有 → 通道态终态。`canRestoreWithoutShizuku()`
  供 UI 决定按钮与文案
- **Manifest**：`WRITE_SECURE_SETTINGS` + tools:ignore="ProtectedPermissions"
- **UI**：HomeUiState +`secureRestoreAvailable`（combine 第 5 路，仍在
  类型化上限内）；按钮条件 `shizukuReady || secureRestoreAvailable`，
  文案跟随实际通道（高级授权在位 →「恢复无障碍授权」，不谎称 Shizuku）；
  刷新点与授权核对同源（refreshAccessibilityState 内顺带，不设常驻观察
  —— install-time 权限运行期几乎不变）。设置页「增强能力」新增
  「高级授权（adb）」行 + AlertDialog（命令含运行时包名注入、无线调试
  引导、小米「USB 调试（安全设置）」提示、状态 ON_RESUME 刷新，
  与电池豁免行同一自治模式——不走 ViewModel）

## 测试（8 例新增）

`SecureSettingsAccessibilityRestorerTest`（内存 map 假体 + 读写记录）：
空设置首恢复（序位断言：先服务条目后 master）/他人保留追加/已存在仅写
master/大小写变体零改写且不重写/静默拒绝→Failed/读 SecurityException→
Failed 且零写入/写 SecurityException→Failed/exec null→首读即终止绝不写。
**一次通过，零返工。**

## 验证证据

```
./gradlew :app:testDebugUnitTest → BUILD SUCCESSFUL
315 tests / 19 类 / 0 failures / 0 errors / 0 skipped
touch 强制重编译（main+test）→ 0 warning（grep "^w: " 无输出）
```

文档同步：plan R13 章节（R12 后、阶段 C 前）；README 双通道小节改写；
MEMORY.md（315/19 基线）；skill §十三 R13 小节。

**阶段 B 至此暂时完结**（S1 全链路 + 自愈 + 保活 + 隐身 + 通知 +
Shizuku 双能力 + 双通道恢复），待用户确认后进入阶段 C（VpnArbitrator /
DnsPacketParser / NoAdVpnService 骨架）。

实机验证点（用户执行）：电脑 `adb shell pm grant com.toaster.noad
android.permission.WRITE_SECURE_SETTINGS` → 设置页「高级授权」显示已授予
→ 制造一次授权丢失 → 首页出现「恢复无障碍授权」（无 Shizuku 亦可）→
点击后自动重连；卸载重装后权限消失属预期。

收口：MEMORY.md 重写至 R13（2998 字符 ≤3000；质量行 315/19、Shizuku 节补
R13 行、文档行 R11–R13），任务 #38 完成。

## R14 自动恢复（2026-09-19 晚）

用户实机验证 R13 后提出：内存清理后仍要手动点按钮恢复，要求「无授权自动
后台获取」。如实定界：force-stop 后进程死，后台唤醒物理不可行（Android
安全设计）；能做满的极限 = 打开应用即自动恢复零点击。

实现（4 文件改 + 2 文件新建 + 2 测试）：
- AutoRestorePolicy：防拉锯节流（30s 间隔 + 每进程 10 次上限），
  进程内状态刻意不持久化（重启清零=重开可立即再试，期望行为），时钟注入
- AccessibilityAutoRestorer：通道门 + 策略 + 执行全注入，JVM 全可测；
  通道缺席不消耗记账
- Controller.restoreViaSecureChannel()：仅 secure 通道公开入口，
  绝不自动走 Shizuku（adb 授权=无感恢复的明确表达；Shizuku force-stop
  后 binder 断，静默调 shell 非预期）
- HomeViewModel.maybeAutoRestoreAuthorization 挂在 refresh 同一 IO 协程；
  触发四条件：应用内 S1 开关开（**用户意图锚**：手动关授权者不会打开
  应用，天然不对抗）+ isNotAuthorized（仅解绑不触发）+ 通道+节流 +
  !fixInFlight；仅 RESTORED 才 toast「检测到授权丢失，已自动恢复」，
  失败静默（卡片+按钮兜底）；恢复后不嵌套 refresh（重绑有时序差）
- NoAdApplication 容器 + accessibilityAutoRestorer lazy（controller 方法引用）
- strings +restore_toast_auto_success

测试：AutoRestorePolicyTest 8 例 + AccessibilityAutoRestorerTest 6 例。
初版 1 失败——givenCustomLimits 用 maxAttempts=1 与间隔判定混淆
（recordAttempt 一次即触上限），改 maxAttempts=2 后通过；顺手删掉
attemptsOf 假断言占位（违背测试语义，改纯行为断言锁「以最近一次为基准」）。

验证：**329 tests / 21 类 / 0 failures / 0 errors**；
compileDebugKotlin + compileDebugUnitTestKotlin touch 强制重编译 **0 warning**。
plan R14 章节、README 自动恢复小节、skill、MEMORY.md 已同步。

实机验证点：adb 授权在位 + 开关开 → 制造授权丢失 → 打开应用 → toast
自动恢复零点击；反向：开关关时制造丢失 → 打开应用不自动恢复。

## 阶段 C 交付（2026-09-19 深夜）：网络层公共模块骨架

方案 = THREE_STRATEGY_PLAN §2.2/§6.9/§4.1/§4.3（唯一方案）。

新建 4 文件：
- core/vpn/VpnArbitrator：启动决策矩阵（S4 共存/降级改写 APP_FIREWALL/
  让位不启动），探测注入 JVM 全测；让位即让位，绝不排队轮询自动重连
  （与 R14 无障碍自动恢复性质不同：VPN 让位涉及接口争抢）
- core/vpn/DnsPacketParser：DNS payload 层解析+构造；拦截=本地 NXDOMAIN、
  上游故障=SERVFAIL 不可混用；改写原查询字节（置 QR/写 RCODE/计数清零/
  长度不变）；防御拒绝：过短/QR=1/opcode≠0/QDCOUNT≠1/压缩指针/截断
- core/vpn/VpnTunConfig：模式→TUN 参数纯函数，S2 铁律（只 route
  10.0.0.1/32 绝不 0.0.0.0/0）从注释升级为可执行断言；OFF/S4 抛错
- core/service/NoAdVpnService 骨架：完整启动决策链 + onRevoke 让位 +
  establish 前读上游 DNS（§4.3①）；**包处理循环属阶段 D**，接入前
  无任何 startService 入口（刻意）；HYBRID 的 VPN 部分=S2 最小路由

改动：Manifest 加 service（BIND_VPN_SERVICE+intent-filter）；容器加
vpnArbitrator/ownVpnNetworkHandle/其他VPN检测（capabilities 主方案+
tun 嗅探兜底）；S4 能力探测恒 false（阶段 E 接线，保守不虚报）。

坑 2 个：NoAdVpnService 忘 import android.net.VpnService；类级 const
val 不合法（常量须在 companion/顶层）。各一次编译修复。

验证：**354 tests / 24 类 / 0 failures / 0 errors**（新增 Arbitrator 7+
Parser 13+TunConfig 5）；强制重编译 0 warning。
plan 阶段 C 列表已标 ✅ + 交付记录小节；skill/MEMORY.md 已同步。
README 未动（骨架无用户可见功能，不加营销文案）。

## 阶段 D 交付（2026-09-19 深夜）：S2 DNS 模式闭环

数据通路：TUN→Ip4UdpPacket（IPv4/UDP 解析）→DnsInterceptor
（引擎白名单优先）→Block=本地 NXDOMAIN / Forward=DnsForwarder
（protect socket）→失败回退 SERVFAIL（§4.3④ 语义区分）。

新组件（core/vpn，全 JVM 可测）：
- Ip4UdpPacket：响应复用原查询包（交换 IP/端口、替换 payload、重算
  IP 校验和）；UDP 校验和置 0（RFC 768 对 IPv4 合法）
- DnsInterceptor：engine 经 () -> 注入（循环用 repo.engine.value）；
  Ignore 仅畸形流量，正常域名一律 Forward 不吞
- DnsForwarder：protectSocket/dnsPort 注入 → localhost 回环闭环测试；
  失败一律 null 绝不抛出

接线：NoAdVpnService 包循环（独立线程阻塞读，onDestroy 关 TUN 自然
退出；FULL_TRAFFIC 拒绝半启动=阶段 E；无上游 DNS 拒绝启动）；容器
ownVpnRunning 短路 detectOtherVpnActive（替代 handle 方案）；VM 走
仲裁→prepare→launcher 流；mode 只在启动动作发出后落盘（UI 真相）；
yielded 改订阅 DataStore（服务 onRevoke 写入页面即时可见，消双真相）；
FULL_TRAFFIC/APP_FIREWALL/HYBRID 不落盘+toast 未接入。

坑 4 个：when 缺 HYBRID 分支（穷尽性）；两处 Byte 字面量缺 .toByte()；
反码校验和测试断言写反（全头折叠=0xFFFF 非 0）；曾写出 MutableStateFlow
假辅助函数与 forwardVia 绕过被测对象的坏测试（自查重写，dnsPort 注入
替代）。All fixed once。

验证：**376 tests / 27 类 / 0 failures / 0 errors**（新增 11+6+5）；
强制重编译 0 warning。plan 阶段 D 标 ✅+交付记录；skill/MEMORY 同步。
