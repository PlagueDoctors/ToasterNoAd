<?xml version="1.0" encoding="utf-8"?>
<!--
    NoAd 应用图标 — 自适应图标前景层

    ## 视觉概念：被切断的广告位

    图形由两部分构成，**均为原创绘制**，未复用任何第三方图标库的路径数据：

    1. **方框**：代表「广告位 / 弹窗」，即被拦截的对象。
       中空描边方形，外沿 26..82，环宽 7，外圆角半径 10。
    2. **斜杠**：代表「拦截」，45 度贯穿的粗带，宽 7。
       在图标正中心 (54,54) 留出缺口，形成「已被切断」的语义 ——
       不是「禁止靠近」，而是「已经切断了」，与产品实际行为一致。

    ## 为什么不用盾牌 / 禁止符号

    - **盾牌**：安全类应用最泛滥的符号，语义也不精确。
      盾牌暗示抵御外部入侵，而广告拦截是在已有流量中做过滤。
    - **禁止符号（圆圈加斜杠）**：语义准确但极度通用，
      缩到 48dp 时与大量「禁用」类应用无法区分。

    「被切断的方框」在极小尺寸下仍保留独特轮廓：方形 + 斜线。
    即使模糊，也能与圆形图标明显区分。

    ## 比例决策

    - 方框描边 7，斜杠宽 7 —— **等宽**。
      早期版本用 9 宽的斜杠，视觉上斜杠压过方框，读起来像
      「一个被划掉的禁止符号」而非「被拦截的广告位」。等宽后两者形成
      结构关系而非覆盖关系。
    - 缺口半宽 5（即总缺口 10，约为斜杠宽的 1.4 倍）。
      太小读不出「切断」，太大则斜杠碎裂成两条无关线段。

    ## 自适应图标安全区

    前景层画布 108x108，但**只有中心 72x72（即 18..90）保证不被裁切**。
    各厂商按圆形 / 方形 / 水滴形等遮罩裁切。

    - **方框 26..82**：完全落在安全区内，任何遮罩下都完整可见。
    - **斜杠延伸至画布外**（-4.47,107.53 与 107.53,-4.47）：有意为之。
      斜杠被遮罩自然裁切后，视觉上读作「穿透了整个图标」。
      裁切只损失长度，不损失「斜杠」这一核心识别特征。

    ## 单色图标（Monochrome）兼容 —— 设计硬约束

    Android 13+ 主题化图标会取本图层的 **alpha 形状**并重新着色。因此：

    1. 图形**只依靠不透明度**传递信息，不依赖颜色差异。
       方框与斜杠同为 `#FFFFFF`。
    2. 缺口依赖「**背景透出**」而非「用背景色填充」。
       这是关键实现选择：若用背景色填充缺口，
       单色模式下缺口会变成一块实色补丁，图形反而被破坏。

    因此本文件**不含任何有色填充**，全部为纯白。

    ## 路径数据由脚本生成

    见 `tools/gen_icon_paths.py`。手写路径时斜杠两段的平行边容易偏差
    1-2 个单位，放大到 512px 时可见明显错位。重新生成只需运行该脚本，
    并将输出替换到下方两个斜杠 path 的 `android:pathData`。

    ⚠️ **同步约束**：修改本文件后必须同步
    `ic_launcher_monochrome.xml`（内容需完全一致）
    与 `tools/gen_icon_pngs.py` 中的几何参数（保证位图与矢量一致）。

    ## 不要使用 android:strokeWidth

    方框的中空效果用「外轮廓 + 内轮廓挖空」的复合路径（`fillType="evenOdd"`）
    表达，而非 `strokeWidth`。
    原因：部分启动器在生成单色主题图标时会丢弃 stroke 属性只保留 fill，
    用 fill 表达的描边能在所有实现下保持形态一致。
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!--
        ============ 方框：被拦截的广告位 ============

        外轮廓顺时针 + 内轮廓逆时针，配合 evenOdd 填充规则挖出中空效果。
        内圆角半径取 3（外圆角 10 减环宽 7），保证方环四角的宽度与四边一致 ——
        若内圆角也取 10，四个角会明显比四边细，看起来像「软掉的方框」。
    -->
    <path
        android:fillColor="#FFFFFFFF"
        android:fillType="evenOdd"
        android:pathData="M36.00,26.00 L72.00,26.00 A10,10 0 0 1 82.00,36.00 L82.00,72.00 A10,10 0 0 1 72.00,82.00 L36.00,82.00 A10,10 0 0 1 26.00,72.00 L26.00,36.00 A10,10 0 0 1 36.00,26.00 Z M36.00,33.00 L33.00,36.00 A3,3 0 0 0 36.00,33.00 L72.00,33.00 A3,3 0 0 0 75.00,36.00 L75.00,72.00 A3,3 0 0 0 72.00,75.00 L36.00,75.00 A3,3 0 0 0 33.00,72.00 L33.00,36.00 A3,3 0 0 0 36.00,33.00 Z" />

    <!--
        ============ 斜杠下半段 ============

        从画布左下角外 (-4.47, 107.53) 延伸到缺口下沿 (52.94, 60.01)。
        带宽 7。缺口中心恰在 (54, 54)，即画布正中。
    -->
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M-4.47,107.53 L47.99,55.06 L52.94,60.01 L0.47,112.47 Z" />

    <!--
        ============ 斜杠上半段 ============

        从缺口上沿 (55.06, 47.99) 延伸到画布右上角外 (112.47, 0.47)。
        与下半段严格共线，中间被挖去 10 单位的缺口。
    -->
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M55.06,47.99 L107.53,-4.47 L112.47,0.47 L60.01,52.94 Z" />
</vector>
